# 파일럿 결과 — 2026-09-23

사용자가 승인한 P01~P04 실행·다운로드를 완료했다. 본 실험은 아직 실행하지 않았다.

| 항목 | 실제 관측 |
|---|---|
| GPU | Tesla T4 |
| 기반 모델 | Qwen/Qwen2.5-0.5B-Instruct |
| 모델 revision | `7ae557604adf67be50417f59c2c2f167def9a775` |
| LoRA | r=8, alpha=16, q_proj/v_proj, dropout=0 |
| 1회 학습 단계 | 0.803초 |
| 최대 GPU 할당량 | 2,185,342,976 bytes (약 2.19 GB) |
| 해당 단계 손실 | 6.1215 |
| EasyOCR 원시 자모 목록 완전 일치 | 0/10 이미지 |
| OCR 검출 자체가 빈 이미지 | 2/10 |
| 설치·기본 검사·GPU 단계·OCR 실행 | 모두 종료 코드 0 |

학습 시간은 모델 다운로드·로딩을 제외한 단일 짧은 샘플의 역전파·업데이트 시간이다. 긴 문구·좌표 보조 과제의 본 학습 시간을 그대로 예측할 수 없다. 손실값은 학습 완료나 생성 정확도를 뜻하지 않는다.

OCR 평가는 고정 글꼴로 합성한 자모 이미지 10개에 한정한다. 원시 검출 문자열에서 공백만 제거한 문자 다중집합과 정답 자모 다중집합을 비교했다. 검출 결과의 한글 음절을 재분해하거나 정답으로 교정하지 않았다. 모든 한국어 OCR의 정확도가 0%라고 해석할 수 없다.

예시:

- `강지수`: OCR 원시 결과 `제 |`.
- `오늘`: 검출 없음.
- `구름`: 검출 없음.

판단: 작은 decoder-only 모델의 추가학습은 실행 가능하다. 현재 EasyOCR 설정은 이 독립 자모 스타일의 학습 주석을 바로 만드는 데 적합하지 않다. 좌표 보조학습 자체를 먼저 검증할지, 자모 전용 OCR을 추가로 개발할지 사용자에게 선택을 요청했다.

원시 기록은 `pilot_received/`에, 문자 목록 비교는 `ocr_pilot_summary.json`에 보관했다. 실행자의 분석이며 별도 Reviewer 검토·사용자의 결과 수락을 대신하지 않는다.
